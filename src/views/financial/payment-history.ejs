<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="icon" type="image/png" href="/imgs/logo.png">
    <title>
        <%= title %> - بوابة بحيرة البردويل
    </title>
</head>

<body>
    <%- include('../partials/navbar') %>

        <main class="main-content">
            <div class="container">
                <!-- Header -->
                <div class="page-header d-flex justify-content-between align-items-center">
                    <div>
                        <h1 class="page-title">
                            <i class="fas fa-history"></i>
                            سجل المدفوعات
                        </h1>
                        <p class="page-subtitle">
                            عرض سجل المدفوعات التي تم التحقق منها
                        </p>
                    </div>
                    <a href="/financial/dashboard" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-right"></i> لوحة التحكم
                    </a>
                </div>

                <!-- Pro Filters -->
                <div class="filters-container mb-4">
                    <div class="filter-card">
                        <div class="filter-header">
                            <i class="fas fa-filter text-primary"></i>
                            <span>تصفية بحسب الفترة</span>
                        </div>
                        <form method="GET" class="filter-form">
                            <div class="search-group">
                                <label class="filter-label">بحث (رقم الطلب / اسم المتقدم)</label>
                                <div class="input-wrapper">
                                    <i class="fas fa-search"></i>
                                    <input type="text" name="search" class="filter-input"
                                        placeholder="رقم الطلب أو اسم المتقدم..."
                                        value="<%= typeof search !== 'undefined' ? search : '' %>">
                                </div>
                            </div>

                            <div class="search-group">
                                <label class="filter-label">فلترة بالمراجع / المحقق</label>
                                <div class="input-wrapper">
                                    <i class="fas fa-user-tie"></i>
                                    <input type="text" name="reviewerFilter" class="filter-input"
                                        placeholder="اسم المراجع أو المحقق..."
                                        value="<%= typeof reviewerFilter !== 'undefined' ? reviewerFilter : '' %>">
                                </div>
                            </div>

                            <div class="date-group">
                                <label class="filter-label">من تاريخ</label>
                                <div class="input-wrapper">
                                    <i class="fas fa-calendar-alt"></i>
                                    <input type="date" name="startDate" class="filter-input" placeholder="من"
                                        value="<%= typeof startDate !== 'undefined' ? startDate : '' %>">
                                </div>
                            </div>

                            <div class="date-group">
                                <label class="filter-label">إلى تاريخ</label>
                                <div class="input-wrapper">
                                    <i class="fas fa-calendar-check"></i>
                                    <input type="date" name="endDate" class="filter-input" placeholder="إلى"
                                        value="<%= typeof endDate !== 'undefined' ? endDate : '' %>">
                                </div>
                            </div>

                            <div class="filter-actions">
                                <button type="submit" class="btn-filter btn-apply">
                                    <i class="fas fa-search"></i> عرض
                                </button>
                                <% if((typeof startDate !=='undefined' && startDate) || (typeof endDate !=='undefined'
                                    && endDate) || (typeof search !=='undefined' && search) || (typeof reviewerFilter
                                    !=='undefined' && reviewerFilter)) { %>
                                    <a href="/financial/payment-history" class="btn-filter btn-reset">
                                        <i class="fas fa-undo"></i> مسح
                                    </a>
                                    <% } %>
                            </div>
                        </form>
                    </div>
                </div>

                <style>
                    .filters-container {
                        background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
                        border-radius: 20px;
                        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
                        padding: 2rem;
                        border: 1px solid rgba(30, 60, 114, 0.1);
                        margin-bottom: 2rem;
                    }

                    .filter-card {
                        background: transparent;
                    }

                    .filter-header {
                        display: flex;
                        align-items: center;
                        gap: 0.75rem;
                        margin-bottom: 1.75rem;
                        font-size: 1.15rem;
                        font-weight: 700;
                        color: #1e3c72;
                        padding-bottom: 1rem;
                        border-bottom: 2px solid linear-gradient(90deg, #1e3c72 0%, #2a5298 100%);
                        border-image: linear-gradient(90deg, #1e3c72, #2a5298) 1;
                    }

                    .filter-header i {
                        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
                        -webkit-background-clip: text;
                        -webkit-text-fill-color: transparent;
                        background-clip: text;
                        font-size: 1.25rem;
                    }

                    .filter-form {
                        display: grid;
                        grid-template-columns: 2fr 1fr 1fr auto;
                        gap: 1.5rem;
                        align-items: end;
                    }

                    @media (max-width: 992px) {
                        .filter-form {
                            grid-template-columns: 1fr 1fr;
                        }
                    }

                    @media (max-width: 576px) {
                        .filter-form {
                            grid-template-columns: 1fr;
                        }
                    }

                    .date-group,
                    .search-group {
                        display: flex;
                        flex-direction: column;
                        gap: 0.6rem;
                    }

                    .search-group {
                        grid-column: span 1;
                    }

                    .filter-label {
                        font-size: 0.85rem;
                        font-weight: 700;
                        color: #1e3c72;
                        text-transform: uppercase;
                        letter-spacing: 0.5px;
                    }

                    .input-wrapper {
                        position: relative;
                        display: flex;
                        align-items: center;
                    }

                    .input-wrapper i {
                        position: absolute;
                        right: 1rem;
                        color: #64748b;
                        z-index: 1;
                        font-size: 1rem;
                        pointer-events: none;
                        transition: color 0.2s ease;
                    }

                    .input-wrapper:focus-within i {
                        color: #1e3c72;
                    }

                    .filter-input {
                        width: 100%;
                        padding: 0.85rem 3rem 0.85rem 1rem;
                        border: 2px solid #e2e8f0;
                        border-radius: 12px;
                        font-family: inherit;
                        font-size: 0.95rem;
                        transition: all 0.25s ease;
                        background: white;
                        color: #1e293b;
                    }

                    .filter-input:hover {
                        border-color: #cbd5e1;
                        background: #f8fafc;
                    }

                    .filter-input:focus {
                        border-color: #1e3c72;
                        background: white;
                        outline: none;
                        box-shadow: 0 0 0 4px rgba(30, 60, 114, 0.12);
                    }

                    .filter-input::placeholder {
                        color: #94a3b8;
                        font-size: 0.9rem;
                    }

                    .filter-actions {
                        display: flex;
                        gap: 0.75rem;
                        align-items: flex-end;
                    }

                    .btn-filter {
                        padding: 0.9rem 1.75rem;
                        border-radius: 12px;
                        font-weight: 700;
                        display: inline-flex;
                        align-items: center;
                        justify-content: center;
                        gap: 0.6rem;
                        cursor: pointer;
                        transition: all 0.25s ease;
                        text-decoration: none;
                        border: none;
                        font-size: 0.95rem;
                        white-space: nowrap;
                    }

                    .btn-apply {
                        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
                        color: white;
                        box-shadow: 0 4px 16px rgba(30, 60, 114, 0.35);
                    }

                    .btn-apply:hover {
                        transform: translateY(-2px);
                        box-shadow: 0 8px 24px rgba(30, 60, 114, 0.45);
                    }

                    .btn-apply:active {
                        transform: translateY(0);
                    }

                    .btn-reset {
                        background: white;
                        color: #64748b;
                        border: 2px solid #e2e8f0;
                    }

                    .btn-reset:hover {
                        background: #f8fafc;
                        border-color: #cbd5e1;
                        color: #475569;
                    }

                    /* Table styling enhancements */
                    .verifier-info {
                        font-size: 0.8rem;
                        line-height: 1.5;
                    }

                    .verifier-info .verifier-row {
                        display: flex;
                        align-items: center;
                        gap: 0.4rem;
                        padding: 0.15rem 0;
                    }

                    .verifier-info .verifier-row.verified {
                        color: #16a34a;
                        font-weight: 600;
                    }

                    .verifier-info .verifier-row.reviewed {
                        color: #64748b;
                    }

                    .verifier-info i {
                        font-size: 0.7rem;
                    }
                </style>

                <!-- Payments Table -->
                <div class="card shadow-sm">
                    <div class="card-header bg-white d-flex justify-content-between align-items-center">
                        <h3 class="card-title h5 mb-0">السجل (<%= pagination.total %>)</h3>
                        <div class="badge bg-success">الإجمالي: <%= totalAmount.toLocaleString('ar-EG') %> ج.م</div>
                    </div>
                    <div class="card-body p-0">
                        <% if (payments.length===0) { %>
                            <div class="empty-state text-center p-5">
                                <i class="fas fa-history text-muted" style="font-size: 3rem; margin-bottom: 1rem;"></i>
                                <h3>لا توجد سجلات</h3>
                                <p class="text-muted">لا توجد مدفوعات مطابقة للفترة المحددة</p>
                            </div>
                            <% } else { %>
                                <div class="table-responsive">
                                    <table class="table table-hover mb-0">
                                        <thead class="bg-light">
                                            <tr>
                                                <th>رقم الطلب</th>
                                                <th>اسم المتقدم</th>
                                                <th>المحقق / المراجع</th>
                                                <th>تاريخ التحقق</th>
                                                <th>المبلغ</th>
                                                <th>الحالة</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% payments.forEach(payment=> { %>
                                                <tr>
                                                    <td>
                                                        <%= payment.applicationNumber %>
                                                    </td>
                                                    <td>
                                                        <%= payment.applicant.firstNameAr %>
                                                            <%= payment.applicant.lastNameAr %>
                                                    </td>
                                                    <td>
                                                        <div style="font-size: 0.8rem; line-height: 1.4;">
                                                            <% if(payment.paymentVerifier) { %>
                                                                <div style="color: #2e7d32;">
                                                                    <i class="fas fa-check-circle"
                                                                        style="font-size: 0.7rem;"></i>
                                                                    <%= payment.paymentVerifier.firstNameAr %>
                                                                        <%= payment.paymentVerifier.lastNameAr %>
                                                                </div>
                                                                <% } %>
                                                                    <% if(payment.reviewer) { %>
                                                                        <div style="color: #666;">
                                                                            <i class="fas fa-user-edit"
                                                                                style="font-size: 0.7rem;"></i>
                                                                            <%= payment.reviewer.firstNameAr %>
                                                                                <%= payment.reviewer.lastNameAr %>
                                                                        </div>
                                                                        <% } else if(!payment.paymentVerifier) { %>
                                                                            -
                                                                            <% } %>
                                                        </div>
                                                    </td>
                                                    <td>
                                                        <%= new
                                                            Date(payment.paymentVerifiedAt).toLocaleDateString('ar-EG')
                                                            %>
                                                    </td>
                                                    <td>
                                                        <%= payment.paymentAmount %> ج.م
                                                    </td>
                                                    <td>
                                                        <span class="badge bg-success">تم التحقق</span>
                                                    </td>
                                                </tr>
                                                <% }); %>
                                        </tbody>
                                    </table>
                                </div>

                                <!-- Pagination -->
                                <% if (pagination.pages> 1) {
                                    const buildUrl = (pageNum) => {
                                    let url = '?page=' + pageNum;
                                    if (typeof startDate !== 'undefined' && startDate) url += '&startDate=' + startDate;
                                    if (typeof endDate !== 'undefined' && endDate) url += '&endDate=' + endDate;
                                    if (typeof search !== 'undefined' && search) url += '&search=' + search;
                                    if (typeof reviewerFilter !== 'undefined' && reviewerFilter) url +=
                                    '&reviewerFilter=' + reviewerFilter;
                                    return url;
                                    }
                                    %>
                                    <div class="card-footer bg-white">
                                        <nav>
                                            <ul class="pagination justify-content-center mb-0">
                                                <% for(let i=1; i <=pagination.pages; i++) { %>
                                                    <li class="page-item <%= pagination.page === i ? 'active' : '' %>">
                                                        <a class="page-link" href="<%= buildUrl(i) %>">
                                                            <%= i %>
                                                        </a>
                                                    </li>
                                                    <% } %>
                                            </ul>
                                        </nav>
                                    </div>
                                    <% } %>
                                        <% } %>
                    </div>
                </div>
            </div>
        </main>

        <style>
            .main-content {
                padding: 2rem 0;
                background: #f8f9fa;
                min-height: 100vh;
            }
        </style>

        <%- include('../partials/footer') %>
</body>

</html>