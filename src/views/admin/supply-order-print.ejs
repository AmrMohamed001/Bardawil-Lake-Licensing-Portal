<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>أمر توريد - <%= data.order.id %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    @page {
      size: A4;
      margin: 1cm;
    }

    body {
      font-family: 'Cairo', 'Segoe UI', Tahoma, sans-serif;
      font-size: 14px;
      line-height: 1.6;
      color: #333;
      background: #fff;
      padding: 20px;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      border: 3px solid #1e3c72;
      padding: 30px;
    }

    /* Header */
    .header {
      text-align: center;
      border-bottom: 2px solid #1e3c72;
      padding-bottom: 20px;
      margin-bottom: 25px;
    }

    .entity-name {
      font-size: 24px;
      font-weight: 700;
      color: #1e3c72;
      margin-bottom: 5px;
    }

    .entity-location {
      font-size: 14px;
      color: #666;
      margin-bottom: 15px;
    }

    .document-title {
      font-size: 28px;
      font-weight: 800;
      color: #00897b;
      background: linear-gradient(135deg, #e0f7fa 0%, #b2dfdb 100%);
      padding: 10px 30px;
      border-radius: 8px;
      display: inline-block;
    }

    /* Order Info */
    .order-info {
      display: flex;
      justify-content: space-between;
      margin-bottom: 25px;
      padding: 15px;
      background: #f5f5f5;
      border-radius: 8px;
    }

    .order-info-item {
      text-align: center;
    }

    .order-info-label {
      font-size: 12px;
      color: #666;
      margin-bottom: 5px;
    }

    .order-info-value {
      font-size: 16px;
      font-weight: 600;
      color: #1e3c72;
    }

    /* Amount Box */
    .amount-box {
      text-align: center;
      padding: 25px;
      background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
      border-radius: 12px;
      color: white;
      margin-bottom: 25px;
    }

    .amount-label {
      font-size: 16px;
      margin-bottom: 10px;
      opacity: 0.9;
    }

    .amount-value {
      font-size: 48px;
      font-weight: 800;
      line-height: 1;
    }

    .amount-currency {
      font-size: 18px;
      margin-top: 5px;
      opacity: 0.9;
    }

    /* Details Section */
    .details-section {
      margin-bottom: 25px;
    }

    .section-title {
      font-size: 16px;
      font-weight: 700;
      color: #1e3c72;
      margin-bottom: 15px;
      padding-bottom: 8px;
      border-bottom: 2px solid #e0e0e0;
    }

    .details-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
    }

    .detail-item {
      display: flex;
      gap: 10px;
    }

    .detail-label {
      font-weight: 600;
      color: #666;
      min-width: 120px;
    }

    .detail-value {
      color: #333;
    }

    /* Instructions */
    .instructions {
      background: #fff3e0;
      border: 2px solid #ff9800;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 25px;
    }

    .instructions-title {
      font-size: 16px;
      font-weight: 700;
      color: #e65100;
      margin-bottom: 15px;
    }

    .instructions-list {
      list-style: none;
      counter-reset: step;
    }

    .instructions-list li {
      counter-increment: step;
      padding: 8px 0;
      border-bottom: 1px dashed #ffcc80;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .instructions-list li:last-child {
      border-bottom: none;
    }

    .instructions-list li::before {
      content: counter(step);
      background: #ff9800;
      color: white;
      width: 24px;
      height: 24px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
      font-weight: 700;
      flex-shrink: 0;
    }

    /* Footer */
    .footer {
      text-align: center;
      padding-top: 20px;
      border-top: 2px solid #e0e0e0;
      color: #666;
      font-size: 12px;
    }

    .print-date {
      margin-top: 10px;
    }

    /* Print Button (hidden in print) */
    .print-actions {
      text-align: center;
      margin-bottom: 20px;
    }

    .print-btn {
      background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
      color: white;
      border: none;
      padding: 12px 40px;
      font-size: 16px;
      font-weight: 600;
      border-radius: 8px;
      cursor: pointer;
      font-family: inherit;
    }

    .print-btn:hover {
      opacity: 0.9;
    }

    @media print {
      .print-actions {
        display: none;
      }

      body {
        padding: 0;
      }

      .container {
        border: none;
      }
    }
  </style>
</head>

<body>
  <div class="print-actions">
    <button class="print-btn" onclick="window.print()">
      🖨️ طباعة أمر التوريد
    </button>
  </div>

  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="entity-name"><%= data.meta.entity %></div>
      <div class="entity-location"><%= data.meta.location %></div>
      <div class="document-title"><%= data.meta.title %></div>
    </div>

    <!-- Order Info -->
    <div class="order-info">
      <div class="order-info-item">
        <div class="order-info-label">رقم أمر التوريد</div>
        <div class="order-info-value"><%= data.order.id %></div>
      </div>
      <div class="order-info-item">
        <div class="order-info-label">رقم الطلب</div>
        <div class="order-info-value"><%= data.application.number %></div>
      </div>
      <div class="order-info-item">
        <div class="order-info-label">تاريخ الإصدار</div>
        <div class="order-info-value"><%= data.meta.generatedAt %></div>
      </div>
    </div>

    <!-- Amount Box -->
    <div class="amount-box">
      <div class="amount-label">المبلغ المطلوب</div>
      <div class="amount-value"><%= data.order.amount %></div>
      <div class="amount-currency"><%= data.order.currency %></div>
    </div>

    <!-- Applicant Details -->
    <div class="details-section">
      <div class="section-title">بيانات مقدم الطلب</div>
      <div class="details-grid">
        <div class="detail-item">
          <span class="detail-label">الاسم:</span>
          <span class="detail-value"><%= data.applicant.name %></span>
        </div>
        <div class="detail-item">
          <span class="detail-label">الرقم القومي:</span>
          <span class="detail-value"><%= data.applicant.nationalId %></span>
        </div>
        <div class="detail-item">
          <span class="detail-label">رقم الهاتف:</span>
          <span class="detail-value"><%= data.applicant.phone %></span>
        </div>
      </div>
    </div>

    <!-- Application Details -->
    <div class="details-section">
      <div class="section-title">بيانات الترخيص</div>
      <div class="details-grid">
        <div class="detail-item">
          <span class="detail-label">نوع الترخيص:</span>
          <span class="detail-value"><%= data.application.type %></span>
        </div>
        <div class="detail-item">
          <span class="detail-label">الفئة:</span>
          <span class="detail-value"><%= data.application.category %></span>
        </div>
      </div>
    </div>

    <!-- Instructions -->
    <div class="instructions">
      <div class="instructions-title">تعليمات الدفع</div>
      <ol class="instructions-list">
        <% data.instructions.forEach(function(instruction) { %>
          <li><%= instruction %></li>
        <% }); %>
      </ol>
    </div>

    <!-- Footer -->
    <div class="footer">
      <div>هذا المستند صادر إلكترونياً من نظام تراخيص بحيرة البردويل</div>
      <div class="print-date">تاريخ الطباعة: <%= new Date().toLocaleDateString('ar-EG', { year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %></div>
    </div>
  </div>

  <script>
    // Auto print on load (optional - can be removed if not desired)
    // window.onload = function() { window.print(); }
  </script>
</body>

</html>
