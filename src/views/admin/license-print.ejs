<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>شهادة ترخيص - <%= data.license.number %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    @page {
      size: A4;
      margin: 1cm;
    }

    body {
      font-family: 'Cairo', 'Segoe UI', Tahoma, sans-serif;
      font-size: 14px;
      line-height: 1.6;
      color: #333;
      background: #fff;
      padding: 20px;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      border: 4px double #00897b;
      padding: 30px;
      background: linear-gradient(135deg, #fafafa 0%, #fff 100%);
    }

    /* Header */
    .header {
      text-align: center;
      border-bottom: 3px solid #00897b;
      padding-bottom: 20px;
      margin-bottom: 25px;
    }

    .entity-name {
      font-size: 26px;
      font-weight: 700;
      color: #1e3c72;
      margin-bottom: 5px;
    }

    .entity-location {
      font-size: 14px;
      color: #666;
      margin-bottom: 15px;
    }

    .document-title {
      font-size: 32px;
      font-weight: 800;
      color: #00897b;
      padding: 15px 40px;
      display: inline-block;
      border: 3px solid #00897b;
      border-radius: 10px;
      background: linear-gradient(135deg, #e0f7fa 0%, #b2dfdb 100%);
    }

    /* License Number Banner */
    .license-banner {
      text-align: center;
      background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
      color: white;
      padding: 20px;
      border-radius: 12px;
      margin-bottom: 25px;
    }

    .license-number-label {
      font-size: 14px;
      opacity: 0.9;
      margin-bottom: 5px;
    }

    .license-number {
      font-size: 32px;
      font-weight: 800;
      letter-spacing: 2px;
    }

    /* Details Grid */
    .details-section {
      margin-bottom: 25px;
    }

    .section-title {
      font-size: 18px;
      font-weight: 700;
      color: #1e3c72;
      margin-bottom: 15px;
      padding-bottom: 8px;
      border-bottom: 2px solid #e0e0e0;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .section-title::before {
      content: '';
      width: 4px;
      height: 20px;
      background: #00897b;
      border-radius: 2px;
    }

    .details-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }

    .detail-card {
      background: #f8f9fa;
      padding: 15px;
      border-radius: 8px;
      border-right: 4px solid #00897b;
    }

    .detail-label {
      font-size: 12px;
      color: #666;
      margin-bottom: 5px;
    }

    .detail-value {
      font-size: 16px;
      font-weight: 600;
      color: #1e3c72;
    }

    /* Validity Section */
    .validity-section {
      display: flex;
      gap: 20px;
      margin-bottom: 25px;
    }

    .validity-card {
      flex: 1;
      text-align: center;
      padding: 20px;
      border-radius: 10px;
    }

    .validity-card.issue {
      background: linear-gradient(135deg, #e8f5e9 0%, #c8e6c9 100%);
      border: 2px solid #4caf50;
    }

    .validity-card.expiry {
      background: linear-gradient(135deg, #fff3e0 0%, #ffe0b2 100%);
      border: 2px solid #ff9800;
    }

    .validity-label {
      font-size: 14px;
      color: #666;
      margin-bottom: 8px;
    }

    .validity-date {
      font-size: 18px;
      font-weight: 700;
      color: #333;
    }

    /* Footer */
    .footer {
      text-align: center;
      padding-top: 20px;
      border-top: 2px solid #e0e0e0;
    }

    .footer-note {
      color: #666;
      font-size: 12px;
      margin-bottom: 10px;
    }

    .print-date {
      color: #999;
      font-size: 11px;
    }

    /* Print Button */
    .print-actions {
      text-align: center;
      margin-bottom: 20px;
    }

    .print-btn {
      background: linear-gradient(135deg, #00897b 0%, #00695c 100%);
      color: white;
      border: none;
      padding: 12px 40px;
      font-size: 16px;
      font-weight: 600;
      border-radius: 8px;
      cursor: pointer;
      font-family: inherit;
    }

    .print-btn:hover {
      opacity: 0.9;
    }

    @media print {
      .print-actions {
        display: none;
      }

      body {
        padding: 0;
      }

      .container {
        border: 4px double #00897b;
      }
    }
  </style>
</head>

<body>
  <div class="print-actions">
    <button class="print-btn" onclick="window.print()">
      🖨️ طباعة شهادة الترخيص
    </button>
  </div>

  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="entity-name"><%= data.meta.entity %></div>
      <div class="entity-location"><%= data.meta.location %></div>
      <div class="document-title"><%= data.meta.title %></div>
    </div>

    <!-- License Number Banner -->
    <div class="license-banner">
      <div class="license-number-label">رقم الترخيص</div>
      <div class="license-number"><%= data.license.number %></div>
    </div>

    <!-- Applicant Details -->
    <div class="details-section">
      <div class="section-title">بيانات صاحب الترخيص</div>
      <div class="details-grid">
        <div class="detail-card">
          <div class="detail-label">الاسم الكامل</div>
          <div class="detail-value"><%= data.applicant.name %></div>
        </div>
        <div class="detail-card">
          <div class="detail-label">الرقم القومي</div>
          <div class="detail-value"><%= data.applicant.nationalId %></div>
        </div>
        <div class="detail-card">
          <div class="detail-label">رقم الهاتف</div>
          <div class="detail-value"><%= data.applicant.phone %></div>
        </div>
      </div>
    </div>

    <!-- License Details -->
    <div class="details-section">
      <div class="section-title">بيانات الترخيص</div>
      <div class="details-grid">
        <div class="detail-card">
          <div class="detail-label">نوع الترخيص</div>
          <div class="detail-value"><%= data.license.type %></div>
        </div>
        <div class="detail-card">
          <div class="detail-label">الفئة</div>
          <div class="detail-value"><%= data.license.category %></div>
        </div>
        <div class="detail-card">
          <div class="detail-label">الرسوم المدفوعة</div>
          <div class="detail-value"><%= data.license.fees %> <%= data.license.currency %></div>
        </div>
      </div>
    </div>

    <!-- Validity Section -->
    <div class="validity-section">
      <div class="validity-card issue">
        <div class="validity-label">تاريخ الإصدار</div>
        <div class="validity-date"><%= data.license.issueDate %></div>
      </div>
      <div class="validity-card expiry">
        <div class="validity-label">صالحة حتى</div>
        <div class="validity-date"><%= data.license.validUntil %></div>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <div class="footer-note"><%= data.footer %></div>
      <div class="print-date">تاريخ الطباعة: <%= new Date().toLocaleDateString('ar-EG', { year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %></div>
    </div>
  </div>
</body>

</html>
